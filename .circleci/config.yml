version: 2
jobs:
  build:
    docker:
    - image: circleci/ruby:2.6.3
    steps:
    - checkout
    - run:
        name: 'Middleman build'
        command: make build

workflows:
  version: 2
  workflow:
    jobs:
    - build