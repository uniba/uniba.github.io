version: 2
jobs:
  build:
    docker:
    - image: circleci/ruby:2.3.4-node
    steps:
    - checkout
    - run:
        name: 'Middleman deploy'
        command: make deploy

workflows:
  version: 2
  workflow:
    jobs:
    - build